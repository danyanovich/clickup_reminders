# SMS Ответы - Быстрый старт

## ✅ Что уже работает

Система автоматически:
1. **Генерирует уникальный код** для каждой задачи (например: `A7K9`)
2. **Отправляет SMS** с кодом и инструкциями, если не дозвонилась
3. **Проверяет входящие SMS** через Twilio API (без webhook!)
4. **Обновляет задачи** в ClickUp по SMS ответам
5. **Отправляет подтверждение** пользователю

## 📱 Пример SMS

**Отправляется системой:**
```
📋 Напоминание: Купить туалетную бумагу

Код: A7K9

Ответьте быстро:
A7K9-1 = Выполнено
A7K9-2 = Не выполнено  
A7K9-3 = В работе
```

**Пользователь отвечает:**
```
A7K9-1
```

**Система подтверждает:**
```
Задача 'Купить туалетную бумагу' отмечена как выполненная ✅
```

## 🚀 Как использовать

### Автоматически (GitHub Actions)

Уже настроено! Каждый час GitHub Actions:
1. Запускает систему напоминаний
2. **Обрабатывает входящие SMS ответы**
3. Обновляет задачи в ClickUp

### Вручную (локально)

```bash
# 1. Отправить напоминания
python reminder_system.py --force

# 2. Обработать SMS ответы
python process_sms_replies.py

# Или оба сразу:
python reminder_system.py --force && python process_sms_replies.py
```

### Автоматически (cron)

```bash
# Добавьте в crontab:
# Каждые 5 минут проверять SMS
*/5 * * * * cd /path/to/ai-reminders && python process_sms_replies.py

# Каждый час - напоминания + SMS
0 * * * * cd /path/to/ai-reminders && python reminder_system.py && python process_sms_replies.py
```

## 🎯 Форматы ответов

Все варианты работают (регистр не важен):

| Ответ | Статус | Результат |
|-------|--------|-----------|
| `A7K9-1` | ВЫПОЛНЕНО | Задача закрывается ✅ |
| `a7k9-1` | ВЫПОЛНЕНО | Работает (авто upper) ✅ |
| `A7K9-2` | НЕ_ВЫПОЛНЕНО | Задача откладывается ❌ |
| `A7K9-3` | В_РАБОТЕ | Задача в работе 🔄 |

## ⚙️ Что происходит

1. **SMS получен** → Twilio API
2. **Парсинг** → `A7K9-1` = код `A7K9`, статус `1`
3. **Поиск задачи** → `var/sms_codes.json`
4. **Обновление ClickUp** → статус + комментарий
5. **Подтверждение** → SMS пользователю

## 📂 Файлы

- `var/sms_codes.json` - маппинг код → задача
- `var/last_processed_sms.txt` - последняя проверка SMS
- `var/logs/reminders_*.log` - логи

## 🔍 Проверка работы

```bash
# Проверить последние SMS
python -c "
from reminder_system import ReminderSystem
system = ReminderSystem()
messages = system.twilio_client.messages.list(to=system.twilio_phone, limit=5)
for m in messages:
    print(f'{m.date_sent}: {m.from_} -> {m.body}')
"

# Проверить сохраненные коды
cat var/sms_codes.json

# Проверить логи
tail -50 var/logs/reminders_$(date +%Y-%m-%d).log | grep SMS
```

## 🎉 Готово!

Система полностью настроена и работает автоматически через GitHub Actions!
