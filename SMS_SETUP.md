# Настройка SMS ответов через Twilio API

Система поддерживает быстрые ответы по SMS с уникальными кодами для каждой задачи.  
**Без необходимости настройки webhook** - проверка SMS идет через Twilio API.

## Как это работает

### 1. Когда система отправляет SMS

Если абонент не ответил на звонок, система отправляет SMS с **уникальным кодом** и инструкциями:

```
📋 Напоминание: Купить туалетную бумагу

Код: A7K9

Ответьте быстро:
A7K9-1 = Выполнено
A7K9-2 = Не выполнено
A7K9-3 = В работе
```

### 2. Пользователь отвечает

Просто отправляет SMS с кодом и цифрой:
- `A7K9-1` → Задача выполнена ✅
- `A7K9-2` → Задача не выполнена ❌
- `A7K9-3` → Задача в работе 🔄

### 3. Система проверяет SMS

Скрипт `process_sms_replies.py` периодически (каждые 5-10 минут) проверяет входящие SMS через Twilio API.

### 4. Система обрабатывает ответ

- Обновляет статус задачи в ClickUp
- Добавляет комментарий с информацией об SMS ответе
- Отправляет подтверждение: "Задача 'Купить туалетную бумагу' отмечена как выполненная ✅"

## Преимущества

✅ **Без webhook** - не нужен публичный URL  
✅ **Быстрый ответ** - не нужно дожидаться звонка  
✅ **Уникальные коды** - можно ответить на несколько напоминаний  
✅ **Автоматическое обновление** - задача обновляется в ClickUp  
✅ **Подтверждение** - получаете ответное SMS  
✅ **История** - все ответы сохраняются в комментариях

## Настройка

### 1. Убедитесь, что все зависимости установлены

```bash
pip install -r requirements.txt
```

### 2. Запустите обработчик SMS вручную

```bash
python process_sms_replies.py
```

### 3. Настройте автоматический запуск через cron

Добавьте в crontab для проверки каждые 5 минут:

```bash
# Редактировать crontab
crontab -e

# Добавить строку (замените путь на свой):
*/5 * * * * cd /path/to/ai-reminders && python process_sms_replies.py >> var/logs/sms_processing.log 2>&1
```

### 4. Или через systemd timer (Linux)

Создайте файл `/etc/systemd/system/sms-processor.service`:

```ini
[Unit]
Description=Process SMS Replies
After=network.target

[Service]
Type=oneshot
User=your-user
WorkingDirectory=/path/to/ai-reminders
ExecStart=/usr/bin/python3 process_sms_replies.py
StandardOutput=append:/path/to/ai-reminders/var/logs/sms_processing.log
StandardError=append:/path/to/ai-reminders/var/logs/sms_processing.log
```

Создайте файл `/etc/systemd/system/sms-processor.timer`:

```ini
[Unit]
Description=Run SMS Processor every 5 minutes

[Timer]
OnBootSec=1min
OnUnitActiveSec=5min

[Install]
WantedBy=timers.target
```

Включите:

```bash
sudo systemctl daemon-reload
sudo systemctl enable sms-processor.timer
sudo systemctl start sms-processor.timer
```

### 5. Добавьте в GitHub Actions

Обновите `.github/workflows/process-recordings.yml`, добавив шаг после `Run reminder system`:

```yaml
- name: Process SMS replies
  run: |
    echo "📱 Обработка входящих SMS..."
    python process_sms_replies.py
  continue-on-error: true
```

## Тестирование

### 1. Запустите систему напоминаний

```bash
python reminder_system.py --force
```

### 2. Проверьте SMS

Вы получите SMS с кодом, например: `A7K9`

### 3. Ответьте на SMS

Отправьте: `A7K9-1` (для выполнено)

### 4. Запустите обработку SMS

```bash
python process_sms_replies.py
```

### 5. Проверьте результат

- Вы получите SMS подтверждение
- Задача обновится в ClickUp
- Появится комментарий

## Пример вывода

```
🚀 Запуск обработки входящих SMS...
[2025-10-17 13:30:15] [INFO] 📅 Последняя проверка: 2025-10-17 13:25:00
[2025-10-17 13:30:15] [INFO] 📥 Получение входящих SMS из Twilio...
[2025-10-17 13:30:16] [INFO] 📊 Найдено 1 новых SMS
[2025-10-17 13:30:16] [INFO] 📱 Входящее SMS от +351939047648: A7K9-1
[2025-10-17 13:30:16] [INFO] ✅ SMS ответ: код=A7K9, задача=86c5z56ut, статус=ВЫПОЛНЕНО
[2025-10-17 13:30:17] [INFO] Задача 86c5z56ut обновлена успешно
[2025-10-17 13:30:17] [INFO] Комментарий с результатами добавлен к задаче 86c5z56ut
[2025-10-17 13:30:18] [INFO] 📤 Отправлено подтверждение на +351939047648
[2025-10-17 13:30:18] [INFO] ✅ Успешно обработано SMS: 1/1
```

## Пример комментария в ClickUp

```markdown
📞 Результат звонка (2025-10-17 13:30:17)
CallSid: SMS-ab12cd34

⚠️ **Запись звонка:** SMS ответ

🗣️ **Транскрипция:**
```
Ответ по SMS от +351939047648: A7K9-1
```
🤖 **AI Анализ:** ВЫПОЛНЕНО
```

## Хранение данных

### SMS коды
Файл: `var/sms_codes.json`

```json
{
  "A7K9": {
    "task_id": "86c5z56ut",
    "task_name": "Купить туалетную бумагу",
    "created_at": "2025-10-17T13:25:00+01:00"
  }
}
```

- Коды автоматически удаляются через **7 дней**
- Каждая задача получает **уникальный код**

### Последняя проверка SMS
Файл: `var/last_processed_sms.txt`

Содержит timestamp последней проверки, чтобы не обрабатывать SMS повторно.

## Безопасность

- ✅ Коды уникальны и короткие (4 символа)
- ✅ Автоматическое истечение через 7 дней
- ✅ Валидация формата ответа
- ✅ Проверка существования кода перед обработкой
- ✅ Без публичного webhook - безопаснее

## Решение проблем

### SMS не обрабатываются

1. **Проверьте запуск скрипта:**
   ```bash
   python process_sms_replies.py
   ```

2. **Проверьте логи:**
   ```bash
   tail -f var/logs/reminders_*.log
   ```

3. **Проверьте Twilio credentials:**
   ```bash
   # Должны быть установлены:
   echo $TWILIO_ACCOUNT_SID
   echo $TWILIO_AUTH_TOKEN
   echo $TWILIO_PHONE_NUMBER
   ```

### Код не распознаётся

1. Убедитесь, что используете правильный формат: `КОД-ЦИФРА`
2. Проверьте, что код не устарел (старше 7 дней)
3. Проверьте регистр - система автоматически конвертирует в верхний регистр

### Задача не обновляется

1. Проверьте доступ к ClickUp API
2. Проверьте переменные окружения (CLICKUP_API_KEY, CLICKUP_TEAM_ID)
3. Проверьте логи для деталей ошибки

### Подтверждение не приходит

1. Проверьте Twilio баланс
2. Проверьте, что номер отправителя верный
3. Проверьте логи Twilio Console

## Мониторинг

### Проверка статуса через логи

```bash
# Последние 20 строк
tail -20 var/logs/reminders_$(date +%Y-%m-%d).log

# Поиск ошибок
grep ERROR var/logs/reminders_*.log

# Статистика обработанных SMS
grep "Успешно обработано SMS" var/logs/reminders_*.log
```

### Проверка через cron

```bash
# Просмотр cron логов
grep CRON /var/log/syslog | tail -20
```
